(function () {
  window.DETAILS = {
     1: {
      title: "Piala Golf Hole in One Club Merapi Golf Yogyakarta",
      desc: `<p>Hole in One Club yang digelar Merapi Golf Yogyakarta menjadi perayaan istimewa untuk mengabadikan pencapaian luar biasa dalam olahraga golf. Ajang ini ditujukan sebagai bentuk penghormatan atas keberhasilan para pegolf yang mampu menorehkan hole-in-one, sebuah prestasi langka penuh kebanggaan dan prestise.</p>
<p>Piala Hole in One Club dihadirkan dengan desain elegan, menampilkan detail pemain golf dan bola yang presisi. <a href="https://troppion.com/piala/" target="_blank" rel="noopener" class="highlight-link">Piala</a> ini diproduksi menggunakan material premium yang dipadukan dengan sentuhan emas mewah sehingga memberikan kesan bernilai tinggi bagi penerimanya.</p>
<p>Serahkan kebutuhan <a href="https://troppion.com/piala-event/piala-olahraga/" target="_blank" rel="noopener" class="highlight-link">Piala Olahraga</a> Anda kepada JogjaPlakat. Kami siap menghadirkan produk berkualitas terbaik yang mampu menonjolkan prestasi Anda secara eksklusif.</p>
`,
    },

    2: {
      title: "Piala Akrilik Monthly Fun Golf Andalan Fluid Sistem",
      desc: `<p>Monthly Fun Golf yang diinisiasi oleh Andalan Fluid Sistem merupakan acara penghargaan untuk mengapresiasi partisipasi serta prestasi para peserta. Diselenggarakan di Kaleang Golf Course, Batulijau, Sumbawa pada November 2024, acara ini bukan hanya kompetisi, melainkan juga ajang mempererat hubungan antar peserta.</p>
<p><a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> yang dihadirkan memiliki desain modern dengan siluet pegolf yang penuh dinamika dan sentuhan logo eksklusif. Terbuat dari akrilik berkualitas tinggi, piala ini memberikan kesan elegan dan berkelas untuk setiap momen penghargaan di dunia golf.</p>
<p>Wujudkan kebutuhan penghargaan Anda bersama <a href="https://troppion.com/" target="_blank" rel="noopener"class="highlight-link">Troppion</a>. Kami berkomitmen menyediakan piala dengan desain premium yang sesuai untuk acara penting Anda.</p>
`,
    },

    3: {
      title: "Piala Golf United Tractors Golfday 2024",
      desc: `<p>United Tractors menghadirkan turnamen Golfday 2024, sebuah kompetisi prestisius yang mempertemukan para pegolf terbaik dalam suasana penuh sportivitas. Ajang ini menjadi kesempatan istimewa untuk menunjukkan kemampuan, dedikasi, sekaligus mempererat hubungan antar peserta.</p>
<p>Piala Golfday 2024 didesain secara elegan, melambangkan kemenangan dengan sentuhan penuh keanggunan. Setiap detailnya menegaskan arti penting sebuah prestasi di arena olahraga golf.</p>
<p>Trofi ini dibuat dari material logam pilihan, memberikan tampilan representatif yang menggambarkan kemegahan turnamen. Untuk piala yang berkesan dan eksklusif, percayakan pada Troppion sebagai penyedia <a href="https://troppion.com/piala/" target="_blank" rel="noopener" class="highlight-link">Piala</a> kustom berkualitas.</p>
`,
    },

    4: {
      title: "Piala Golf The Majestic Challenge Hosel Cup 2.0 Yogyakarta",
      desc: `<p>The Majestic Challenge Hosel Cup 2.0 Yogyakarta merupakan turnamen golf eksklusif yang mempertemukan para pegolf terbaik dengan tantangan penuh gengsi. Ajang ini menjadi momen penting di mana para peserta bersaing untuk meraih kemenangan prestisius.</p>
<p><a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> yang diperebutkan mencerminkan keunggulan teknik serta strategi bermain. Kompetisi ini bukan sekadar menguji keterampilan, namun juga daya tahan mental di lapangan hijau.</p>
<p>Dibuat dari bahan kristal premium, <a href="https://troppion.com/" target="_blank" rel="noopener"class="highlight-link">Troppion</a> menghadirkan desain elegan penuh prestise. Jadikan piala ini sebagai simbol kejayaan dan abadikan kemenangan Anda dengan trofi eksklusif dari Troppion.</p>
`,
    },

    5: {
      title: "Piala Golf Hosel Cup Kategori Best Net Overall",
      desc: `<p>Golf kerap dianggap sebagai olahraga berkelas, sehingga tidak heran bila setiap turnamennya identik dengan nuansa elegan. Hal tersebut tercermin jelas dalam Piala Bhayangkara Golf Tournament 2024 yang didesain penuh kemewahan.</p>
<p><a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> ini dirancang menggunakan material logam pilihan sehingga tampil lebih kokoh dan tahan lama. Warna emas yang mendominasi semakin menambah nilai eksklusif dan prestisius dari trofi ini.</p>
<p>Bagi Anda yang menginginkan piala berkesan, tersedia layanan personalisasi. Segera sampaikan kebutuhan desain <a href="https://troppion.com/piala/" target="_blank" rel="noopener" class="highlight-link">Piala</a> Anda kepada tim customer service kami untuk mendapatkan hasil terbaik.</p>
`,
    },

    6: {
      title: "Piala Golf Bhayangkara Golf Tournament 2024",
      desc: `<p>Hosel Cup kategori Best Nett Overall merupakan salah satu ajang golf bergengsi yang memperebutkan <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> istimewa. Kompetisi ini menjadi bukti atas keunggulan strategi dan kemampuan bermain para pegolf terbaik.</p>
<p>Piala tersebut didesain secara khusus untuk mencerminkan makna mendalam dari kemenangan. Dibuat dari logam pilihan, trofi ini menghadirkan kesan anggun sekaligus tahan lama.</p>
<p>Desain yang elegan pada <a href="https://troppion.com/piala/" target="_blank" rel="noopener" class="highlight-link">Piala</a> ini menggambarkan prestise turnamen bergengsi. Wujudkan trofi kustom Anda bersama Troppion untuk mengabadikan pencapaian berharga di dunia golf.</p>
`,
    },

    7: {
      title: "Piala Golf Badak LNG Golf Course Bontang Best Nett",
      desc: `<p>Golf selalu identik dengan kemewahan, sehingga setiap turnamennya terasa lebih eksklusif. Hal tersebut tergambar jelas pada Piala Badak LNG Golf yang diberikan sebagai penghargaan kategori Best Nett.</p>
<p>Piala ini dibuat menggunakan material akrilik berkualitas yang dipadukan dengan teknik modern. Hasilnya, <a href="https://troppion.com/piala/" target="_blank" rel="noopener" class="highlight-link">Piala</a> tampil berkelas dan representatif untuk turnamen bergengsi.</p>
<p>Anda juga bisa memesan versi kustom piala ini. Hubungi customer service Troppion untuk mewujudkan <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> sesuai kebutuhan event Anda.</p>
`,
    },

    8: {
      title:
        "Piala Golf Longest Drive Jasa Pertambangan Fun Golf 2024 Kuta Bali",
      desc: `<p>Fun Golf 2024 di Kuta, Bali, kategori Longest Drive menjadi ajang unjuk kebolehan pegolf dalam menghasilkan pukulan terjauh. Selain kompetitif, acara ini juga menghadirkan suasana santai penuh keakraban.</p>
<p>Piala penghargaan khusus kategori Longest Drive ini didesain oleh Troppion menggunakan akrilik berkualitas tinggi. <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> ini mencerminkan elegansi serta prestasi luar biasa dari para pemenang.</p>
<p>Bagi Anda yang ingin memiliki piala custom, Troppion siap menyediakan desain sesuai kebutuhan. Segera hubungi customer service untuk mendapatkan <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> unik dan berkesan bagi event Anda.</p>
`,
    },

    9: {
      title: "Trophy Golf Akrilik Best Gross Merapi Golf Yogyakarta",
      desc: `<p>Best Gross Merapi Golf Yogyakarta merupakan penghargaan bergengsi bagi pegolf dengan skor terendah. Trofi ini menjadi simbol dedikasi, keterampilan, sekaligus kecintaan mendalam terhadap olahraga golf.</p>
<p>Dibuat khusus oleh Troppion, Trophy Golf Akrilik ini hadir dengan kejernihan material premium. <a href="https://troppion.com/piala/" target="_blank" rel="noopener" class="highlight-link">Piala</a> ini tampil menawan dan layak dijadikan penghargaan bergengsi.</p>
<p>Troppion juga menawarkan layanan custom <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a>. Dengan desain yang bisa disesuaikan, setiap kemenangan Anda akan lebih berkesan dan diabadikan dalam trofi elegan.</p>
`,
    },

    10: {
      title: "Piala Golf Akrilik Nearset Merapi Golf Yogyakarta",
      desc: `<p>Turnamen Nearset Merapi Golf Yogyakarta menjadi ajang prestisius yang mempertemukan pegolf amatir maupun profesional. Selain kompetisi, acara ini juga sarana membangun relasi sambil menikmati keindahan alam Merapi.</p>
<p>Troppion menghadirkan <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> berbahan akrilik berkualitas tinggi dengan tampilan jernih serta kilau elegan. Piala ini menjadi penghargaan yang diidamkan setiap peserta.</p>
<p>Bagi penyelenggara yang ingin memberikan penghargaan istimewa, Troppion menawarkan layanan custom <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a>. Dengan desain unik, setiap prestasi akan terekam secara berkesan.</p>
`,
    },

    11: {
      title: "Piala Tenee Open Tournament Golf – BNO dan BGO",
      desc: `<p>Turnamen Tenee Open di Royal Sumatera Golf & Country Medan menjadi ajang bergengsi yang mempertemukan pegolf berbakat dari berbagai wilayah. Troppion turut berkontribusi dengan menghadirkan <a href="https://troppion.com/piala/" target="_blank" rel="noopener" class="highlight-link">Piala</a> custom khusus untuk kompetisi ini.</p>
<p>Piala dirancang dari logam premium dengan detail elegan yang menegaskan kemewahan turnamen. Setiap desain dibuat untuk merepresentasikan keagungan ajang BNO dan BGO sekaligus menjadi simbol prestasi.</p>
<p>Bila Anda ingin memiliki <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> custom eksklusif, hubungi Troppion. Kami siap menyediakan trofi unik yang bernilai historis dan estetis untuk momen kemenangan Anda.</p>
`,
    },

    12: {
      title: "Piala Golf Tribute To Our Best Regional CEO BRI Semarang",
      desc: `<p>Tribute To Our Best Regional CEO BRI Semarang merupakan momen penghargaan istimewa untuk kepemimpinan dan dedikasi luar biasa. Dalam acara ini, Troppion mempersembahkan <a href="https://troppion.com/piala-set/" target="_blank" rel="noopener" class="highlight-link">Piala Set</a> khusus sebagai simbol penghormatan.</p>
<p>Piala berbahan resin berkualitas tinggi ini menghadirkan detail halus dan finishing elegan. <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> ini tak hanya sekadar penghargaan, namun juga karya seni bernilai.</p>
<p>Jadikan acara penghargaan Anda lebih bermakna dengan piala custom dari Troppion. Kami siap mewujudkan <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> yang elegan dan penuh prestise sesuai kebutuhan Anda.</p>
`,
    },

    13: {
      title:
        "Piala Best Gross Overall – Serikat Pekerja Pertamina Patra Wijayakusuma (SPP PWK)",
      desc: `<p>Best Gross Overall SPP PWK merupakan perhelatan prestisius yang dirayakan dengan piala custom dari Troppion. Ajang ini menjadi penanda penting bagi pencapaian tertinggi para pegolf.</p>
<p>Piala dirancang dari logam pilihan dengan detail estetis yang menawan. Desainnya mencerminkan simbol keberhasilan sekaligus menjadi karya seni dalam bentuk <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> berkelas.</p>
<p>Untuk menghadirkan penghargaan yang abadi, Troppion menawarkan layanan pembuatan <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> custom dari logam. Jadikan kemenangan Anda lebih berkesan dengan trofi elegan ini.</p>
`,
    },

    14: {
      title: "Trophy Golf Go Bar Baruna Bina Utama – Longest Drive",
      desc: `<p>Longest Drive Go Bar Baruna Bina Utama merupakan kategori paling bergengsi dalam golf yang menantang pegolf mencetak pukulan terjauh. Ajang ini menuntut kekuatan, strategi, serta ketelitian.</p>
<p>Trofi Longest Drive dirancang dari logam berkualitas tinggi dengan desain elegan yang mencerminkan keunggulan prestasi. <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> ini menjadi simbol kebanggaan bagi pemenang.</p>
<p>Rayakan pencapaian luar biasa Anda dengan <a href="https://troppion.com/piala-event/piala-olahraga/piala-golf/" target="_blank" rel="noopener" class="highlight-link">Piala Golf</a> custom dari Troppion. Kami siap menghadirkan trofi eksklusif yang disesuaikan dengan kebutuhan event Anda.</p>
`,
    },
  };

  /* ===================== 2) GAMBAR BERURUTAN ===================== */
  const IMG_SEQ_CONFIG = {
    basePath: "assets/images",
    prefix: "golf",
    start: 1,
    total: 27,
    ext: "jpg", // default: mayoritas file kamu .jpg
    exts: ["webp", "jpg", "jpeg", "png", "gif"], // urutan fallback
  };

  // yang .webp: 1–3
  const EXT_BY_INDEX = { 100: "webp", 72: "webp", 83: "webp" };

  function urlForImage(idx, cfg = IMG_SEQ_CONFIG, extOverride) {
    const chosenExt = extOverride || EXT_BY_INDEX[idx] || cfg.ext;
    return `${cfg.basePath}/${cfg.prefix}${idx}.${chosenExt}`;
  }

  /** set <img> dengan fallback ke daftar ekstensi */
  function setImageWithFallback(imgEl, idx, cfg = IMG_SEQ_CONFIG) {
    // mulai dari ext khusus (kalau ada), lalu coba sisa exts
    const firstExt = EXT_BY_INDEX[idx] || cfg.ext;
    const queue = [firstExt, ...cfg.exts.filter((e) => e !== firstExt)];
    let i = 0;

    function tryLoad() {
      imgEl.src = urlForImage(idx, cfg, queue[i]);
    }
    imgEl.onerror = () => {
      i++;
      if (i < queue.length) tryLoad();
      else {
        imgEl.onerror = null;
        imgEl.src =
          "https://images.unsplash.com/photo-1519681393600-7e08d64b9dc3?q=80&w=1200&auto=format&fit=crop";
      }
    };
    tryLoad();
  }

  function assignSequentialImages(cfg = IMG_SEQ_CONFIG) {
    const cards = Array.from(document.querySelectorAll(".product-card"));
    let idx = cfg.start;
    for (let i = 0; i < cards.length; i++) {
      if (idx > cfg.start - 1 + cfg.total) break;
      const card = cards[i];
      const btn = card.querySelector("[data-open-detail]");
      if (!btn) continue;

      const hasImg = (btn.getAttribute("data-img") || "").trim().length > 0;
      if (!hasImg) {
        const url = urlForImage(idx, cfg);
        btn.setAttribute("data-img", url);
        const thumb = card.querySelector(".product-thumb");
        if (thumb) thumb.src = url;
        idx++;
      }
    }
  }

  /* ===================== 3) UTIL ===================== */
  const DEFAULT_IMG =
    "https://images.unsplash.com/photo-1519681393600-7e08d64b9dc3?q=80&w=1200&auto=format&fit=crop";

  const slugify = (s = "") =>
    s
      .toString()
      .toLowerCase()
      .normalize("NFD")
      .replace(/[\u0300-\u036f]/g, "")
      .replace(/[^a-z0-9]+/g, "-")
      .replace(/(^-|-$)+/g, "");

  const escapeHtml = (str = "") =>
    str.replace(
      /[&<>"']/g,
      (m) =>
        ({
          "&": "&amp;",
          "<": "&lt;",
          ">": "&gt;",
          '"': "&quot;",
          "'": "&#039;",
        }[m])
    );

  /* ===================== 4) RENDER KARTU ===================== */
  function ensureCardThumbs() {
    const cards = document.querySelectorAll(".product-card");
    cards.forEach((card, idx) => {
      const btn = card.querySelector("[data-open-detail]");
      if (!btn) return;

      // Prioritas judul: DETAILS[data-id].title → data-title → H3 → "Produk X"
      const id = btn.dataset.id?.trim();
      const titleFromDetails = id && DETAILS[id]?.title;
      const dataTitle = btn.getAttribute("data-title")?.trim();
      const h3Existing = card.querySelector("h3")?.textContent?.trim();

      const title =
        titleFromDetails || dataTitle || h3Existing || `Produk ${idx + 1}`;

      const img = btn.getAttribute("data-img")?.trim() || DEFAULT_IMG;

      if (!card.id) card.id = `produk-${slugify(title)}`;

      const h3 = card.querySelector("h3");
      if (h3) {
        h3.textContent = title;
        h3.classList.add(
          "font-heading",
          "font-semibold",
          "text-lg",
          "product-title"
        );
      }

      if (!card.querySelector(".product-thumb")) {
        const wrap = card.querySelector(".reveal") || card;
        const imgEl = document.createElement("img");
        imgEl.className = "product-thumb w-full rounded-xl object-contain bg-white";
        imgEl.style.objectFit = "contain"; // pastikan pakai contain
        imgEl.style.borderRadius = "0"; // biar menempel ke pinggir
        imgEl.loading = "lazy";
        imgEl.src = img;
        imgEl.alt = title;
        wrap.prepend(imgEl);
      }
    });
  }

  /* ===================== 5) ANIMASI KARTU ===================== */
  const cards = Array.from(document.querySelectorAll(".product-card"));
  if (!("IntersectionObserver" in window) || cards.length === 0) {
    cards.forEach((c) => c.classList.add("in-view"));
  } else {
    const io = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const el = entry.target;
            const idx = cards.indexOf(el);
            el.style.transitionDelay = 80 * (idx % 8) + "ms";
            el.classList.add("in-view");
            io.unobserve(el);
          }
        });
      },
      { threshold: 0.2 }
    );
    cards.forEach((card) => io.observe(card));
  }

  /* ===================== 6) MODAL (Gambar Zoom + Judul + Deskripsi) ===================== */
  const modal = document.getElementById("detailModal");
  const titleEl = document.getElementById("detailTitle");
  const imgEl = document.getElementById("detailImg"); // akan dibungkus zoom-wrap
  const descEl = document.getElementById("detailDesc");
  const closeBtn = document.getElementById("closeModalBtn");

  // (opsional) Jika modal lama masih punya rating/keyword/kelebihan/kekurangan di HTML, hapus:
  (function removeExtraSections() {
    const ratingEl = document.getElementById("detailRating");
    if (ratingEl) {
      const grid = ratingEl.closest(".grid");
      if (grid) grid.remove();
    }
    const kwWrap = document.getElementById("detailKeywords")?.parentElement;
    if (kwWrap) kwWrap.remove();
  })();

  // Zoom state
  let zoom = { scale: 1, min: 1, max: 3, step: 0.25, panX: 0, panY: 0 };
  let zoomWrap;

  function wrapImageWithZoom() {
    if (!imgEl || imgEl.closest(".zoom-wrap")) return;
    zoomWrap = document.createElement("div");
    zoomWrap.className =
      "zoom-wrap relative overflow-hidden rounded-xl bg-gray-100";
    imgEl.parentNode.insertBefore(zoomWrap, imgEl);
    zoomWrap.appendChild(imgEl);

    const ctrls = document.createElement("div");
    ctrls.className = "zoom-controls absolute top-3 right-3 flex gap-2";
    ctrls.innerHTML = `
      <button type="button" class="zoom-btn rounded-md border px-2 py-1 bg-white" data-zoom="-">−</button>
      <button type="button" class="zoom-btn rounded-md border px-2 py-1 bg-white" data-zoom="+">+</button>
      <button type="button" class="zoom-btn rounded-md border px-2 py-1 bg-white" data-zoom="reset">Reset</button>
    `;
    zoomWrap.appendChild(ctrls);

    ctrls.addEventListener("click", (e) => {
      const z = e.target?.dataset?.zoom;
      if (z === "+") zoomIn();
      else if (z === "-") zoomOut();
      else if (z === "reset") resetZoom();
    });

    // Wheel-zoom
    zoomWrap.addEventListener(
      "wheel",
      (e) => {
        e.preventDefault();
        e.deltaY < 0 ? zoomIn() : zoomOut();
      },
      { passive: false }
    );

    // Double click reset
    imgEl.addEventListener("dblclick", resetZoom);

    // Drag-to-pan
    let isDown = false,
      startX = 0,
      startY = 0;
    zoomWrap.addEventListener("mousedown", (e) => {
      if (zoom.scale <= 1) return;
      isDown = true;
      zoomWrap.style.cursor = "grabbing";
      startX = e.clientX - zoom.panX;
      startY = e.clientY - zoom.panY;
    });
    window.addEventListener("mouseup", () => {
      isDown = false;
      zoomWrap.style.cursor = "";
    });
    zoomWrap.addEventListener("mousemove", (e) => {
      if (!isDown || zoom.scale <= 1) return;
      zoom.panX = e.clientX - startX;
      zoom.panY = e.clientY - startY;
      applyTransform();
    });
  }

  function applyTransform() {
    imgEl.style.transform = `translate(${zoom.panX}px, ${zoom.panY}px) scale(${zoom.scale})`;
    imgEl.style.transformOrigin = "center center";
    imgEl.style.transition = "transform 120ms ease";
  }
  function zoomIn() {
    zoom.scale = Math.min(zoom.max, zoom.scale + zoom.step);
    if (zoom.scale <= 1) zoom.panX = zoom.panY = 0;
    applyTransform();
  }
  function zoomOut() {
    zoom.scale = Math.max(zoom.min, zoom.scale - zoom.step);
    if (zoom.scale <= 1) zoom.panX = zoom.panY = 0;
    applyTransform();
  }
  function resetZoom() {
    zoom.scale = 1;
    zoom.panX = 0;
    zoom.panY = 0;
    applyTransform();
  }

  function openModal({ id, title, img, desc, cardId }) {
    // Jika ada ID & ditemukan di DETAILS, override title & desc dari sana
    if (id && DETAILS[id]) {
      title = DETAILS[id].title || title;
      desc = DETAILS[id].desc || desc;
    }

    titleEl.textContent = title || "";
    imgEl.src = img || DEFAULT_IMG;
    imgEl.alt = title || "Gambar produk";

    // Jika desc berasal dari DETAILS, itu HTML aman; kalau dari data-desc biasa → escape
    const isHtmlDesc = !!(id && DETAILS[id] && DETAILS[id].desc);
    const contentDesc = isHtmlDesc
      ? desc || ""
      : desc
      ? `<p>${escapeHtml(desc)}</p>`
      : "";

    const backlink = cardId
      ? `<br><br><a href="#${cardId}" class="detail-backlink text-brand underline font-semibold">Lihat produk di daftar</a>`
      : "";

    descEl.innerHTML = `${contentDesc}${backlink}`;

    resetZoom();
    modal.classList.remove("hidden");
    modal.setAttribute("aria-hidden", "false");
    document.body.style.overflow = "hidden";
  }

  function closeModal() {
    modal.classList.add("hidden");
    modal.setAttribute("aria-hidden", "true");
    document.body.style.overflow = "";
  }

  // Delegasi klik tombol "Detail"
  document.addEventListener("click", (e) => {
    const btn = e.target.closest("[data-open-detail]");
    if (!btn) return;
    const card = btn.closest(".product-card");
    const id = btn.dataset.id?.trim(); // <-- ambil ID untuk lookup DETAILS
    const dataTitle =
      btn.getAttribute("data-title") ||
      card.querySelector("h3")?.textContent ||
      "";
    const img = btn.getAttribute("data-img") || DEFAULT_IMG;
    const dataDesc = btn.getAttribute("data-desc") || "";
    openModal({ id, title: dataTitle, img, desc: dataDesc, cardId: card?.id });
  });

  // Close handlers
  closeBtn?.addEventListener("click", closeModal);
  modal?.addEventListener("click", (e) => {
    if (e.target === modal || e.target.classList.contains("bg-black/50"))
      closeModal();
  });
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape" && !modal.classList.contains("hidden")) closeModal();
  });

  // ====== INIT (urutan penting) ======
  assignSequentialImages(); // isi data-img otomatis: assets/images/golf1.jpg, dst.
  ensureCardThumbs(); // render thumbnail + judul pada kartu
  wrapImageWithZoom(); // aktifkan zoom di modal
})();
(function () {
  const cvs = document.getElementById("goldParticles");
  const ctx = cvs.getContext("2d");
  const wrap = document.getElementById("trophyWrap");
  const img = document.getElementById("trophyImg");
  const dpr = Math.max(1, Math.min(window.devicePixelRatio || 1, 2));

  let w = 0,
    h = 0,
    rect = null;
  let particles = [];
  let last = performance.now();

  // ====== PARAMETER TUNING ======
  const BASE_COUNT_DESKTOP = 110;
  const BASE_COUNT_MOBILE = 70;
  const GLOW_TRAIL = 0.22; // jejak (0=tidak ada, 1=hilang total)
  const MAX_DIST_FACTOR = 0.65; // seberapa jauh dari piala (0.4-0.8)
  const CENTER_PULL = 0.015; // tarikan ke pusat
  const WANDER = 0.35; // besaran gerak acak
  const SPEED = 32; // px/detik

  const isMobile = () => matchMedia("(max-width: 768px)").matches;

  function resizeCanvas() {
    rect = wrap.getBoundingClientRect();
    w = rect.width;
    h = rect.height;
    cvs.style.width = w + "px";
    cvs.style.height = h + "px";
    cvs.width = Math.floor(w * dpr);
    cvs.height = Math.floor(h * dpr);
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
  }

  function getCenterAndRadius() {
    const i = img.getBoundingClientRect();
    const wr = wrap.getBoundingClientRect();
    const cx = (i.left + i.right) / 2 - wr.left;
    const cy = (i.top + i.bottom) / 2 - wr.top;
    // radius orbit dasar mengikuti ukuran piala
    const rx = i.width * MAX_DIST_FACTOR;
    const ry = i.height * MAX_DIST_FACTOR;
    return { cx, cy, rx, ry };
  }

  function spawnOne() {
    const { cx, cy, rx, ry } = getCenterAndRadius();
    // mulai di dekat elips piala
    const t = Math.random() * Math.PI * 2;
    const rj = 0.65 + Math.random() * 0.6; // variasi jarak
    const x = cx + Math.cos(t) * rx * rj;
    const y = cy + Math.sin(t) * ry * rj;

    const hue = 45 + Math.random() * 12;
    const size = 1.2 + Math.random() * 2.2;

    return {
      x,
      y,
      vx: (Math.random() - 0.5) * 10,
      vy: (Math.random() - 0.5) * 10,
      tw: Math.random() * Math.PI * 2, // fase kedip
      twSpeed: 2 + Math.random() * 3.5, // kecepatan kedip
      hue,
      sat: 90 + Math.random() * 10,
      light: 60 + Math.random() * 20,
      alpha: 0.65 + Math.random() * 0.35,
      size,
      sparkle: Math.random() < 0.18,
    };
  }

  function spawnAll() {
    particles = [];
    const count = isMobile() ? BASE_COUNT_MOBILE : BASE_COUNT_DESKTOP;
    for (let i = 0; i < count; i++) particles.push(spawnOne());
  }

  function step(dt) {
    const { cx, cy, rx, ry } = getCenterAndRadius();
    const maxR = Math.max(rx, ry) * 1.15;

    for (let i = particles.length - 1; i >= 0; i--) {
      const p = particles[i];

      // gerak acak (kunang-kunang): random walk + tarikan halus ke pusat
      p.vx += (Math.random() - 0.5) * WANDER;
      p.vy += (Math.random() - 0.5) * WANDER;

      p.vx += (cx - p.x) * CENTER_PULL * dt;
      p.vy += (cy - p.y) * CENTER_PULL * dt;

      // batasi kecepatan supaya lembut
      const maxV = SPEED;
      const vmag = Math.hypot(p.vx, p.vy);
      if (vmag > maxV) {
        p.vx = (p.vx / vmag) * maxV;
        p.vy = (p.vy / vmag) * maxV;
      }

      p.x += p.vx * dt;
      p.y += p.vy * dt;

      // jika terlalu jauh, teleport halus dekat orbit lagi
      const dist = Math.hypot(p.x - cx, p.y - cy);
      if (dist > maxR * 1.6) {
        const t = Math.random() * Math.PI * 2;
        p.x = cx + Math.cos(t) * maxR * 0.9;
        p.y = cy + Math.sin(t) * maxR * 0.9;
        p.vx *= 0.2;
        p.vy *= 0.2;
      }

      // update kedip
      p.tw += p.twSpeed * dt;
    }
  }

  function draw() {
    // fade agar ada trail lembut
    ctx.save();
    ctx.globalCompositeOperation = "destination-out";
    ctx.fillStyle = `rgba(0,0,0,${GLOW_TRAIL})`;
    ctx.fillRect(0, 0, w, h);
    ctx.restore();

    // gambar partikel
    particles.forEach((p) => {
      // flicker halus (0.75–1) + sparkle sesekali
      const flick = 0.75 + 0.25 * Math.sin(p.tw);
      const sparkleBoost = p.sparkle
        ? 0.25 + 0.75 * (Math.sin(p.tw * 3) * 0.5 + 0.5)
        : 0;
      const a = Math.min(1, p.alpha * flick + sparkleBoost * 0.25);

      // glow: lingkaran inti + halo
      ctx.globalCompositeOperation = "lighter";

      // halo
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.size * 3.2, 0, Math.PI * 2);
      ctx.fillStyle = `hsla(${p.hue}, ${p.sat}%, ${Math.min(
        90,
        p.light + 20
      )}%, ${a * 0.08})`;
      ctx.fill();

      // inti
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
      ctx.fillStyle = `hsla(${p.hue}, ${p.sat}%, ${p.light}%, ${a})`;
      ctx.fill();

      ctx.globalCompositeOperation = "source-over";
    });
  }

  function loop(now) {
    const dt = Math.min(0.05, (now - last) / 1000); // batas 50ms
    last = now;
    step(dt);
    draw();
    requestAnimationFrame(loop);
  }

  function start() {
    resizeCanvas();
    spawnAll();
    last = performance.now();
    requestAnimationFrame(loop);
  }

  window.addEventListener("resize", () => {
    resizeCanvas();
    // jaga jumlah partikel konsisten saat resize
    spawnAll();
  });

  if (img.complete) start();
  else img.addEventListener("load", start);
})();

// ====== PARALLAX RINGAN UNTUK BACKGROUND ======
(function () {
  const root = document.getElementById("bg-anim");
  if (!root) return;

  const blobs = root.querySelectorAll(".blob");
  const aurora = root.querySelector(".aurora");
  let ticking = false;

  function onScroll() {
    if (ticking) return;
    ticking = true;
    requestAnimationFrame(() => {
      const y = window.scrollY || 0;
      const f = Math.min(1, y / 1200); // batas pengaruh
      // gerakkan pelan ke bawah (parallax)
      aurora && (aurora.style.transform = `translate3d(0, ${f * 6}px, 0)`);
      blobs.forEach((b, i) => {
        const k = i % 2 ? 10 : 6; // variasi kedalaman
        b.style.transform = `translate3d(0, ${f * k}px, 0)`; // scale/rotate tetap dari keyframes
      });
      ticking = false;
    });
  }
  window.addEventListener("scroll", onScroll, { passive: true });
  onScroll(); // initial
})();
/* ===== MODAL DETAIL PRODUK (paste di akhir script.js) ===== */
(function () {
  // Kalau modal belum ada di DOM, jangan jalan
  const modal = document.getElementById('detailModal');
  if (!modal) return;

  // Nomor WA tujuan
  const WA_NUMBER = '6285728527934';

  // Elemen-elemennya
  const backdrop   = document.getElementById('modalBackdrop');
  const closeBtn   = document.getElementById('closeModalBtn');
  const closeBtn2  = document.getElementById('detailClose2');

  const titleEl    = document.getElementById('detailTitle');
  const nameEl     = document.getElementById('detailName');
  const priceEl    = document.getElementById('detailPrice');
  const imgEl      = document.getElementById('detailImg');
  const descEl     = document.getElementById('detailDesc');
  const orderBtn   = document.getElementById('detailOrderBtn');

  // DETAILS boleh sudah ada dari file ini juga; kalau belum, inisialisasi kosong
  window.DETAILS = window.DETAILS || {};
  const DEFAULT_IMG = 'https://images.unsplash.com/photo-1519681393600-7e08d64b9dc3?q=80&w=1200&auto=format&fit=crop';

  // Utils
  const escapeHtml = (s='') => s.replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
  const buildWaLink = (name, price) => {
    const text = `Halo Troppion, saya ingin pesan ${name || 'produk ini'}${price ? ' ('+price+')' : ''}.`;
    return `https://wa.me/${WA_NUMBER}?text=${encodeURIComponent(text)}`;
  };

function openModal(payload){
  const { id, dataTitle, img, dataDesc, cardId } = payload;

  // Cari dari DETAILS
  const fromDetails = (id && window.DETAILS && DETAILS[id]) ? DETAILS[id] : null;

  const title = (fromDetails?.title || dataTitle || '').trim();
  const descHTML = fromDetails?.desc
      ? fromDetails.desc // <-- ini HTML langsung dari DETAILS
      : (dataDesc ? `<p>${escapeHtml(dataDesc)}</p>` : '');

  titleEl.textContent = title || 'Detail Produk';
  nameEl.textContent  = title || '';
  priceEl.textContent = dataPrice || '';
  imgEl.src           = img || DEFAULT_IMG;
  imgEl.alt           = title || 'Gambar produk';

  // render deskripsi
  const backlink = cardId ? `<br><br><a href="#${cardId}" class="detail-backlink">Lihat produk di daftar</a>` : '';
  descEl.innerHTML = `${descHTML}${backlink}`;

  // link WA
  orderBtn.href = buildWaLink(title, dataPrice);

  modal.classList.remove('hidden');
  modal.setAttribute('aria-hidden','false');
  document.body.style.overflow = 'hidden';
}


  function closeModal(){
    modal.classList.add('hidden');
    modal.setAttribute('aria-hidden','true');
    document.body.style.overflow = '';
  }

  // Delegasi klik tombol “Detail”
  document.addEventListener('click', (e)=>{
    const btn = e.target.closest('[data-open-detail]');
    if(!btn) return;

    const card = btn.closest('.product-card');
    const payload = {
      id:        (btn.dataset.id || '').trim(),
      dataTitle: btn.getAttribute('data-title') || card?.querySelector('h3')?.textContent || '',
      dataPrice: btn.getAttribute('data-price') || '',
      img:       btn.getAttribute('data-img') || '',
      dataDesc:  btn.getAttribute('data-desc') || '',
      cardId:    card?.id || ''
    };
    openModal(payload);
  });

  // Tutup modal
  closeBtn?.addEventListener('click', closeModal);
  closeBtn2?.addEventListener('click', closeModal);
  backdrop?.addEventListener('click', closeModal);
  document.addEventListener('keydown', (e)=>{ if(e.key==='Escape' && !modal.classList.contains('hidden')) closeModal(); });
})();
